"use client";
import Image from "next/image";import Link from "next/link";import { useState } from "react";import { portalFetch } from "@/lib/client/portal-fetch";
export function PaymentConfirmation({registrationReference,studentName,className,tuition,paymentStatus}:{registrationReference:string;studentName:string;className:string;tuition:string;paymentStatus:string}){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const[errors,setErrors]=useState<Record<string,string>>({});async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");setErrors({});const form=new FormData(event.currentTarget);form.set("registrationReference",registrationReference);try{const response=await portalFetch("/api/payments",{method:"POST",body:form});const data=await response.json() as {message?:string;fieldErrors?:Record<string,string>;nextUrl?:string};if(!response.ok){setErrors(data.fieldErrors||{});throw new Error(data.message||"Payment information could not be saved.");}setMessage(data.message||"Payment submitted.");setTimeout(()=>window.location.assign(data.nextUrl||"/account/dashboard"),1300);}catch(error){setMessage(error instanceof Error?error.message:"Payment information could not be saved.");setBusy(false);}}return <section className="section-shell py-12 sm:py-16"><div className="mx-auto max-w-4xl rounded-3xl border border-white/10 bg-gradient-to-br from-black via-stone-950 to-black p-6 shadow-2xl shadow-black/40 sm:p-10"><div className="text-center"><p className="text-xs font-black uppercase tracking-[0.28em] text-red-300">Secure payment submission</p><h1 className="hero-title mt-4 text-4xl font-bold text-white sm:text-5xl">Complete Your Payment</h1><p className="mt-4 text-sm font-bold text-red-200">Registration reference: {registrationReference}</p></div><div className="mt-8 grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><div><div className="rounded-2xl border border-white/10 bg-white p-5"><Image src="/images/Paypal.JPG" alt="PayPal QR code for SHOTOKAN Karate Regina tuition payment" width={600} height={600} priority className="h-auto w-full object-contain"/></div><p className="mt-4 text-center text-sm leading-6 text-stone-400">Scan the PayPal QR code and keep your receipt. Submission does not confirm payment until an administrator verifies it.</p></div><div><div className="grid gap-3">{[["Student",studentName],["Selected class",className],["Tuition",tuition],["Payment status",paymentStatus]].map(([label,value])=><div className="rounded-xl border border-white/10 bg-white/[0.04] p-4" key={label}><p className="text-xs font-black uppercase tracking-[0.12em] text-red-300">{label}</p><p className="mt-1 text-white">{value}</p></div>)}</div>{paymentStatus==="Not Paid"?<form className="mt-6" onSubmit={submit}><label className="text-sm font-semibold text-stone-200">PayPal transaction reference<input name="transactionReference" className={`mt-2 w-full rounded-xl border bg-white/5 px-4 py-3 text-white outline-none ${errors.transactionReference?"border-red-400":"border-white/10"}`} placeholder="Transaction ID from your PayPal receipt"/>{errors.transactionReference?<p className="mt-2 text-sm text-red-300">{errors.transactionReference}</p>:null}</label><label className="mt-5 block text-sm font-semibold text-stone-200">Receipt upload <span className="font-normal text-stone-500">(optional · JPG, PNG, or PDF · max 2 MB)</span><input name="receipt" type="file" accept="image/jpeg,image/png,application/pdf" className={`mt-2 block w-full rounded-xl border bg-white/5 p-3 text-sm text-stone-300 file:mr-4 file:rounded-md file:border-0 file:bg-red-600 file:px-4 file:py-2 file:text-white ${errors.receipt?"border-red-400":"border-white/10"}`}/>{errors.receipt?<p className="mt-2 text-sm text-red-300">{errors.receipt}</p>:null}</label>{message?<p role="alert" className="mt-5 rounded-xl border border-white/10 bg-white/5 p-4 text-sm text-stone-200">{message}</p>:null}<button disabled={busy} className="mt-6 min-h-12 w-full rounded-md bg-red-600 px-5 text-sm font-black uppercase tracking-[0.12em] text-white disabled:opacity-60">{busy?"Submitting…":"I Have Completed My Payment"}</button></form>:<p className="mt-6 rounded-xl border border-amber-400/20 bg-amber-950/25 p-4 text-sm leading-7 text-amber-100">Payment information has already been submitted. Current status: {paymentStatus}.</p>}</div></div><div className="mt-8 text-center"><Link className="text-sm font-bold text-red-300" href="/account/dashboard">Return to Family Dashboard</Link></div></div></section>}
