import { notFound } from "next/navigation";import { StudentForm } from "@/components/account/StudentForm";import { requireAccount } from "@/lib/portal/auth";import { readPortalDatabase } from "@/lib/portal/store";
export default async function Page({params}:{params:Promise<{id:string}>}){const account=await requireAccount({verified:true});const{id}=await params;const student=(await readPortalDatabase()).students.find((item)=>item.id===id&&item.accountId===account.id);if(!student)notFound();return <div className="section-shell"><div className="mx-auto max-w-4xl"><StudentForm account={account} student={student}/></div></div>}
