import Link from "next/link";import { AccountSettings } from "@/components/account/AccountSettings";import { requireAccount } from "@/lib/portal/auth";import { readPortalDatabase } from "@/lib/portal/store";
export default async function Page(){const account=await requireAccount();const contact=(await readPortalDatabase()).emergency_contacts.find((item)=>item.accountId===account.id);return <div className="section-shell"><div className="mb-7"><Link href="/account/dashboard" className="text-sm font-bold text-red-300">← Family Dashboard</Link><h1 className="hero-title mt-4 text-4xl font-bold text-white">Account Settings</h1></div><AccountSettings account={account} contact={contact}/></div>}
