import Link from "next/link";import { AdminRegistrationActions } from "@/components/AdminRegistrationActions";import { requireAccount } from "@/lib/portal/auth";import { money } from "@/lib/portal/security";import { readPortalDatabase } from "@/lib/portal/store";
export const dynamic="force-dynamic";export default async function Page(){await requireAccount({verified:true,admin:true});const db=await readPortalDatabase();const enrollments=[...db.enrollments].sort((a,b)=>b.createdAt.localeCompare(a.createdAt));return <main className="min-h-screen bg-stone-950 px-4 py-20 text-white sm:px-6"><div className="mx-auto max-w-7xl"><div className="mb-8 rounded-3xl border border-white/10 bg-black/60 p-8"><p className="text-xs font-black uppercase tracking-[0.28em] text-red-300">Role-protected administration</p><h1 className="mt-3 text-3xl font-black uppercase sm:text-4xl">Enrollment & Payment Review</h1><p className="mt-3 text-stone-300">Only verified administrator accounts can confirm payment and activate enrollments.</p><Link className="mt-5 inline-block text-sm text-red-300" href="/account/dashboard">← Family Dashboard</Link></div><div className="overflow-x-auto rounded-2xl border border-white/10 bg-black/50"><table className="min-w-[1050px] w-full text-left text-sm"><thead className="bg-white/5 text-stone-300"><tr>{["Submitted","Reference","Account","Student","Class","Tuition","Enrollment","Payment","Transaction / Receipt","Actions"].map((heading)=><th className="px-4 py-3" key={heading}>{heading}</th>)}</tr></thead><tbody className="divide-y divide-white/10">{enrollments.map((entry)=>{const account=db.accounts.find((item)=>item.id===entry.accountId),student=db.students.find((item)=>item.id===entry.studentId),karateClass=db.classes.find((item)=>item.id===entry.classId),payment=db.payments.filter((item)=>item.enrollmentId===entry.id).sort((a,b)=>b.submittedAt.localeCompare(a.submittedAt))[0];return <tr className="align-top" key={entry.id}><td className="whitespace-nowrap px-4 py-4">{new Date(entry.createdAt).toLocaleString("en-CA")}</td><td className="px-4 py-4 font-bold text-red-200">{entry.registrationReference}</td><td className="px-4 py-4">{account?.firstName} {account?.lastName}<br/><span className="text-stone-400">{account?.email}</span></td><td className="px-4 py-4">{student?.fullName}</td><td className="px-4 py-4">{karateClass?.name}</td><td className="px-4 py-4">{money(entry.tuitionCents)}</td><td className="px-4 py-4">{entry.enrollmentStatus}</td><td className="px-4 py-4">{entry.paymentStatus}</td><td className="px-4 py-4">{payment?.transactionReference||"—"}{payment?.receiptId?<><br/><a className="text-red-300" href={`/api/receipts/${payment.receiptId}`} target="_blank">Private receipt</a></>:null}</td><td className="px-4 py-4"><AdminRegistrationActions id={entry.id} paymentStatus={entry.paymentStatus}/></td></tr>})}</tbody></table>{!enrollments.length?<p className="p-8 text-stone-400">No enrollments have been received.</p>:null}</div></div></main>}
